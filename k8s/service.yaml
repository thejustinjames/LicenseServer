apiVersion: v1
kind: Service
metadata:
  name: license-server
  labels:
    app: license-server
spec:
  type: ClusterIP
  selector:
    app: license-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
---
# Optional: LoadBalancer service for external access
# Uncomment if not using an Ingress controller
# apiVersion: v1
# kind: Service
# metadata:
#   name: license-server-lb
#   labels:
#     app: license-server
#   annotations:
#     # AWS ALB annotations
#     service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
#     service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
# spec:
#   type: LoadBalancer
#   selector:
#     app: license-server
#   ports:
#     - name: http
#       port: 80
#       targetPort: http
#       protocol: TCP
---
# Optional: Headless service for StatefulSet or direct pod access
# apiVersion: v1
# kind: Service
# metadata:
#   name: license-server-headless
#   labels:
#     app: license-server
# spec:
#   type: ClusterIP
#   clusterIP: None
#   selector:
#     app: license-server
#   ports:
#     - name: http
#       port: 3000
#       targetPort: http
